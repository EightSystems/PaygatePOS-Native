export default class NetworkImplementation {
    static listDevices() {
        return Promise.reject();
    }

    static isEnabled() {
        return Promise.reject();
    }

    static enable() {
        return Promise.reject();
    }

    static disable() {
        return Promise.reject();
    }

    static connect(device) {
        return Promise.reject();
    }

    static disconnect() {
        return Promise.reject();
    }

    static isConnected() {
        return Promise.reject();
    }

    static write(buffer) {
        return Promise.reject();
    }
}
