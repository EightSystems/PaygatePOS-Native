export default class BluetoothImplementation {
    static listDevices() {
        return Promise.reject();
    }

    static isEnabled() {
        return Promise.reject();
    }

    static enable() {
        return Promise.reject();
    }

    static disable() {
        return Promise.reject();
    }

    static connect(device) {
        return Promise.reject();
    }
}
